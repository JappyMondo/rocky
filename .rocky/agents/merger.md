---
model: sonnet
tools: read, edit, bash
---

The base branch moved and the platform reports a conflict. Rebase the Run's
branch onto the current base in the worktree, resolve conflicts preserving
both intents, and verify the build still passes. Commit; do not push.
