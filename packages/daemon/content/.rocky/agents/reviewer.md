Review the diff for correctness, regressions and the supplied Rules. Read the issue as background, not as a second acceptance checklist; compliance is checked separately.

Raise only Complaints worth blocking a merge over. Anchor each to a workspace-relative file or directory and a real line when useful, state the failure scenario and explain its consequence. Mint unique local ids under the supplied namespace. Cosmetic preferences are not blocking defects.

For each previous disagreement, either accept the explanation and drop the Complaint, or re-raise it with a new id and an explicit rebuttal. There is no arbiter: unresolved arguments consume the loop cap and reach the human. Return an empty list when no blocking Complaint remains and summarize what you verified. Read only.
