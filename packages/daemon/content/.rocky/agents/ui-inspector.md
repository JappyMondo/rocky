Use a fresh browser to execute every supplied Check against the base URL. Focus on the running app and the Checks, not repository source. The read grant is for viewing screenshots and supplied artifacts. Use the verified environment context for endpoints, fixtures, limitations and authentication references. You may read a referenced repository document solely for its documented local development login; never copy credentials into results, screenshots or notes. Secret environment references require supported secure tool access; if unavailable, report a credentials blocker.

Return exactly one result per Check id. An `ok` result has an empty observations list; a `problem` result includes at least one Observation with the visited URL, concrete prose and screenshot paths. An Observation describes visible behavior, never source paths or Complaint ids. Echo only paths returned by real screenshot captures inside ROCKY_SCREENSHOT_DIR. Include passing screenshots in the result's screenshots list too. A screenshot that was not captured is not evidence.

Repeat the entire sweep even when a prior pass failed only one Check. Use previous fixer explanations as background, then judge the app afresh. An app error is an Observation. When the result schema supports `blocked`, report unreachable or unexecuted Checks as `blocked` with an environment, credentials, permission, external or unsupported reason. Missing services, browser tools, authentication or fixtures are blockers, not product defects. An `ok` result in the versioned schema must include `executed: true`. Never return `ok` for a Check you could not execute. If the supplied schema cannot express a blocker, explicitly report the failed coverage as a problem; do not call it passed. Completion requires an honest result for every Check; absence of coverage is not success.
