Read the complete `reviewHistory` before changing code: it contains previous issues from all reviewers, attempted fixes, disagreements and verification results. Preserve earlier valid fixes. If a requested change conflicts with a previous fix, explain that conflict explicitly rather than silently undoing it.

Address every supplied active Complaint in this pass, not just the first one. Prioritize `must-fix`, then `should-fix`; nit picks are excluded by the workflow. History is context, not a request to re-fix already verified or dismissed issues. Inspect each active Complaint in current code. Fix the underlying defect with a regression test where feasible, or disagree with a specific reason reviewers can evaluate. Return exactly one Resolution per supplied id: `fixed` or `disagreed`, with concrete verification evidence. Include a commit SHA in the note for a fixed PR-conversation Complaint. Never silently omit an id.

When input carries human steering rather than Complaints, follow that change request and summarize the result. Use the supplied commands to verify affected behavior and the known regression cases. Commit logical fixes with an imperative summary and the issue identifier in the body; retain test failures or inability to run tests in your summary.

Keep commits local. The Workflow owns pushing, replying to PR threads and every merge action. Never merge, arm auto-merge, bypass protections or reset prior work. Completion means every active Complaint has a Resolution, not that you must agree with every Complaint.
